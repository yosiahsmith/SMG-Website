import Link from 'next/link';
import careers from '../../data/careers.json';

export default function Careers(){
  const openRoles = careers.roles.filter(role => role.active);
  return <main className="page"><div className="shell"><span className="eyebrow">CAREERS</span><h1>Build what comes next.</h1><p className="lead">We're building a company around measurable growth, practical AI, and systems that make businesses better. See our current openings below.</p><section className="steps">{openRoles.length ? openRoles.map((role,index)=><div className="step" key={role.id}><div className="stepno">{String(index+1).padStart(2,'0')}</div><div><span className="tag">{role.department} · {role.type}</span><h3>{role.title}</h3><p>{role.description}</p><p className="muted">{role.location}</p><a className="textlink" href={`mailto:${role.applyEmail}?subject=${encodeURIComponent(`Application: ${role.title}`)}`}>Apply for this role →</a></div></div>) : <div className="card"><h3>No open positions right now.</h3><p>We don't currently have an active opening, but we're always interested in exceptional people.</p><Link className="btn" href="mailto:careers@solomedia.group">Introduce yourself</Link></div>}</section><div className="card"><span className="tag">OWNER CONTROLS</span><h3>Want to change the openings?</h3><p>The careers page is driven by one simple configuration file in the GitHub repository. Turn roles on or off, add new positions, change titles, descriptions, departments, locations, or application emails without rebuilding the page.</p><p className="muted"><code>data/careers.json</code></p></div></div></main>}
